{
    "title": "Sick Day Oracle Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "datasetId": {
            "title": "Dataset ID",
            "type": "string",
            "description": "The ID of the dataset from the Weather Oracle actor.",
            "editor": "textfield"
        },
        "jobTitle": {
            "title": "Your job title",
            "type": "string",
            "description": "What you do as your main job.",
            "editor": "textfield"
        }
    },
    "required": ["datasetId", "jobTitle"]
}